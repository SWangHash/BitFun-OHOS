{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-dialog",
  "description": "Native dialog permissions for desktop platforms where the dialog plugin is available",
  "windows": ["main", "agent-companion-pet", "spotlight"],
  "permissions": ["dialog:default"],
  "platforms": ["macOS", "windows", "linux"]
}
